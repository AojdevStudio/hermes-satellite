# Docs validation

Run `pnpm docs:validate` before submitting documentation changes. The command checks local links/assets, public-safe placeholders, required operational-safety language, malformed snippets, accessible search/copy controls, mobile navigation, and representative static page coverage. Use `<bridge-host>`, `100.x.x.x`, and `${HERMES_MCP_TOKEN}` in public examples.
